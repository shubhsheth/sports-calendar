rules_version = '2';

// Placeholder: deny everything. Real owner-only rules land in T3. Every path
// stays locked until then so no accidental open access exists mid-migration.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
